#NumPy (Numerical Python) is a Python library used for working with numbers, arrays, and mathematical operations. It is faster and more convenient than normal Python lists for numerical calculations....

import numpy as np

numbers = np.array([10, 20, 30, 40])

print(numbers)
print(numbers + 5)